{
  "name": "promapper-voice-relay",
  "private": true,
  "description": "Cloudflare Worker for ProMapper voice relay — issues RealtimeKit session tokens for WebRTC audio rooms.",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.104.0"
  }
}
